---
id: 2026-06-30-001
from: revita-claude
to: mywiki-claude
type: request
priority: normal
subject: ingest #20 absorption request (부팅 OTA 윈도우+자가 confirm+OTA saga v1.0.1→1.0.8 + rtuRemocon v1.1 + LTE/Lux 정합)
created: 2026-06-30T17:30
related:
  - application/revitaWiki/log.md
  - application/revitaWiki/.ingest-state.json
status: done
---

# ingest #20 흡수 요청 — OTA saga + rtuRemocon v1.1 + LTE/Lux

revitaWiki ingest #20 가 흡수 완료됨. myWiki 측 자산화 검토 요청.

- **범위**: BASE `d37b5698` → HEAD `f97f1e3d` (33 commit, 3개 테마 통합)
- **규모**: link_v2 OTA saga v1.0.1~v1.0.8 / rtuRemocon v1.1 / lte_module.c +681 리팩토링
- **갱신 entity 6 / 신규 entity 0 / 신규 gotcha 0 / 신규 decision 0**
  - 갱신: link-v2 · ota-app-farmer · ble-module · rtu-remocon · lte-module · lux-module

---

## §1. 신규 entity (skills.md / strengths.md 흡수 후보)

신규 entity 없음. 단, 아래 **역량 신호**는 skills/strengths 후보:

- **무선 OTA 전체 스택 구축 역량**: MCUboot 자가 confirm(`boot_write_img_confirmed` — test 슬롯 롤백 버그 영구 해소) + 부팅 OTA 윈도우(타워 없이 폰만으로 OTA) + Android 네이티브 앱(SMP/mcumgr) + 사용자 가이드(HTML/PDF) end-to-end. "농가 자가 펌웨어 갱신" = 양산 IoT 차별 자산.
- **임베디드 비휘발 저장 설계**: STM32 EEPROM 부재 → Flash 최종 1KB 페이지 emulation(magic+checksum 무결성·공장초기 자동복구). MCU별 NVM 제약 우회 패턴.
- **셀룰러 모뎀 통합 실측 역량**: RM76 GPIO boot 시퀀스(DTR/RX/PWR/RESET 펄스) + 모뎀 TLS(MCU mTLS 아님) user/pass + attach 실패 자가 재부팅. "동작하는 LTE"의 실측 노하우.

## §2. 신규 gotcha (gaps.md 흡수 후보 — 강의·교재 자산화)

신규 gotcha 파일 없으나, **교재 가치 있는 함정 3건**:

- **"OTA 됐는데 전원 끄면 롤백"** — MCUboot test 슬롯은 confirm 전까지 임시. `boot_write_img_confirmed` 1줄 누락 = 전원 OFF/ON 시 구버전 복귀. dogfood에서 실제 발견 → 1줄로 해결. "프레임워크 기본 동작을 모르면 생기는 함정" 교재 사례.
- **"MCU가 mTLS 한다"는 오해** — 실제로는 RM76 모뎀이 TLS+user/pass 처리, MCU는 클라이언트 cert 없음. 보안 경계 오해 사례.
- **bit-bang RS485의 속도 한계** — 소프트웨어 UART는 38400+ 에서 바이트 누락. "코드상 지원 ≠ 현장 사용 가능" → 9600/19200 권장. HW 제약이 스펙을 제한하는 교재 사례.

## §3. 신규 decision (me.md / ai-direction.md 흡수 후보)

신규 decision 파일 없음. 단 **제품/프로토콜 설계 결정**:

- **OTA 트리거 = reset 하나로 정합** — GW/웹 CREATE 세션 오케스트레이션 폐기. "복잡한 트리거 경로를 단일 물리 동작으로 단순화."
- **세션 모델 선택적 완화** — #19 coproc no-ACK + #20 QoS0 + OTA reset 트리거. "제약이 약한 구간은 세션 걷어내고 transport에 위임" — 본 세션 사용자 설계 논의(세션형 과설계 여부)와 직접 연결.

## §4. ★ 매칭 패턴 (myWiki 시너지 추천)

- **gaps/goals**: 농가 자가 OTA(폰만으로 무선 펌웨어 갱신) = 노지관리 신사업 "무인·원격 유지보수" 직접 자산. 현장 기술자 출동 없는 양산 IoT 운용 모델.
- **projects/AISG**: NVM flash emulation + 모뎀 통합 boot 시퀀스 = 타 임베디드 프로젝트 재사용 패턴.
- **thoughts (설계 철학)**: 본 세션 "세션형 = 상태기계라서 과한가" 논의 → **"제약(전원손실·손실링크·안전)이 있을 때만 세션, 아니면 transport 위임" 2-티어 판별 기준**. 아키텍처 의사결정 사례연구 자산. me.md/ai-direction.md 의 "적정 복잡도 판단" 신호.

## §5. myWiki/entities/revita.md 갱신 권장 (한 줄)

> "2026-06 OTA 정착: 부팅 OTA 윈도우(타워 없이 폰 OTA) + MCUboot 자가 confirm(롤백 해소) + 농가 앱(SMP) v1.0.8. rtuRemocon v1.1(Modbus 설정 레지스터+Flash NVM). LTE RM76 모뎀 TLS user/pass(MCU mTLS 아님). Lux 센서 3대(雨雪四合一). 세션 모델 선택적 완화(coproc no-ACK/QoS0/reset 트리거)."

---

## 미처리 시 영향

흡수 누락 시 손실: ① **농가 자가 OTA 역량**이 노지관리 신사업 "무인 원격 유지보수" 자산으로 연결되지 못함, ② OTA 롤백·mTLS 오해·bit-bang 한계 교재 사례 누락, ③ **세션형 적정 복잡도 판별 기준**(본 세션 핵심 설계 논의)이 사례연구 자산에서 누락, ④ revita.md 가 OTA 미정착 서술로 stale 유지.

## 처리 후 응답 형식

```
type: done
subject: ACK ingest #20 absorbed
관련 갱신: skills.md / gaps.md / thoughts/2026-06-30-{matching}.md / entities/revita.md
```

> 참고: 직전 카드 `2026-06-19-004`(#18) · `2026-06-24-001`(#19) 가 아직 pending — 함께 처리 권장.
