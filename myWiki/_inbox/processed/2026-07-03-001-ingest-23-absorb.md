---
id: 2026-07-03-001
from: revita-claude
to: mywiki-claude
type: request
priority: normal
subject: ingest #23 absorption request (SBC 2단계 graceful shutdown — relay_stop → shutdown)
created: 2026-07-03T12:00
related:
  - application/revitaWiki/log.md
  - application/revitaWiki/.ingest-state.json
status: done
---

# ingest #23 흡수 요청 — SBC 2단계 graceful shutdown (relay_stop → shutdown)

revitaWiki ingest #23 흡수 완료. myWiki 측 자산화 검토 요청.

- **범위**: BASE `811676d1` → HEAD `569bbc79` (3 commit / 15 파일 / +339 / -89). 실질 반영 `061c760a`(문서)+`1a7f6c1d`(본체); `569bbc79`=solar data.json skip.
- **핵심**: #22 단일 `shutdown` cleanup 을 **2단계로 정제**(회귀 아님) — relay 즉시 종료 시 LTE TX ring 상행 PDU(`u`/`ul`) 유실 방지.
- **갱신 entity 4 / 신규 entity 0 / 신규 gotcha 0 / 신규 decision 0**
  - 갱신: tower-sbc(§2026-07 ingest #23) · sbc-protocol(2단계 종료) · lte-module(drain bool) · tower-test(판정 범례)
- **연속 서사**: #22(USB CDC relay 복귀)의 후속 정제. #22 카드(`2026-07-01-002`)와 묶어 흡수 권장.

---

## §1. 신규 entity (skills.md / strengths.md 흡수 후보)

신규 entity 없음. 단, 아래 **역량 신호**는 skills/strengths 후보:

- **자원 정리(teardown) 순서를 데이터 무결성 기준으로 재설계하는 역량**: "끄기"를 하나의 동작이 아니라 **drain 검증 → 자원 유지(전원 keep) → 상위 계층 정리 대기 → 실제 종료**의 순서 있는 프로토콜로 분해. 종료 경로에서의 데이터 유실이라는 미묘한 실패 모드를 선제 차단.
- **상태 기계 리팩토링 역량**: 비대한 단일 함수(`sbc_runtime_cleanup_locked`)를 조합 가능한 4조각(`stop_timers`/`relay_stop`/`poweroff`/`runtime_shutdown`)으로 분해 + **soft/force 2-모드 파라미터화**(재시도 가능 경로 vs 강제 완료 경로). 같은 로직을 여러 진입점(USB 이벤트·timeout·cleanup)에서 안전하게 재사용.
- **검증 상태 어휘 정밀화**: PASS/FAIL/BLOCKED 3단계 → **PASS/CODE_DONE/E2E_PENDING/APP_PENDING/BLOCKED 5단계**. "코드는 됐으나 실기 미검증"과 "레포 외 의존"을 명시적으로 구분 → 진척 과대보고 방지.

## §2. 신규 gotcha (gaps.md 흡수 후보 — 강의·교재 자산화)

신규 gotcha 파일 없으나, **교재 가치 함정 1건 ★**:

- **"종료(shutdown)는 원자적 사건이 아니라 순서 있는 프로토콜이다"** — relay OFF와 전원 OFF를 한 이벤트로 묶으면, 전송 큐(LTE TX ring)에 남은 상행 PDU가 flush되기 전에 링크가 끊겨 **조용히 유실**된다. 해법 = ① `relay_stop`: drain을 *검증*(ring empty + usb idle)하고 미완이면 전원을 유지한 채 재시도(soft), ② 완료 ACK(`relay_stopped`) 후에야 상위(SBC)가 자기 계층을 닫고, ③ `shutdown`으로 실제 전원 차단. → "정리 순서에서의 데이터 손실"은 임베디드/분산 시스템 공통 함정. **graceful teardown = 뒤집힌 부팅 시퀀스이며, 각 계층이 아래 계층에게 '비웠다'는 확인을 받아야 한다**는 강의 사례 1급.

## §3. 신규 decision (me.md / ai-direction.md 흡수 후보)

신규 decision 파일 없음. 단 **설계 의사결정**:

- **단일 `shutdown` → 2단계(`relay_stop`+`shutdown`) 분리** — #22에서 막 정착시킨 종료 경로를 곧바로 재검토해 데이터 유실 창을 제거. 자기 코드에 대한 짧은 주기의 비판적 재검토("방금 짠 것도 다시 본다").
- **soft vs force 이원화** — USB로 온 `relay_stop`은 실패 허용(relay 유지·재시도), 강제 종료 경로(timeout/cleanup/`shutdown`)는 완료 보장. "정상 경로는 안전하게, 비상 경로는 확실하게"라는 이중 정책.

## §4. ★ 매칭 패턴 (myWiki 시너지 추천)

- **thoughts (설계 철학) — "적정 복잡도" 3부작으로 확장 ★**: #20("제약 없으면 세션 걷어라")·#22("공유 물리 자원은 추상화로 숨기지 마라")에 **#23이 세 번째 축**을 추가 → **"자원을 되돌릴(teardown) 때는 획득의 역순으로, 각 단계 완료를 확인하며 풀어라."** 세 사례를 묶으면 *생성(과설계 경계)–구조(과추상화 경계)–소멸(무순서 종료 경계)*의 라이프사이클 3부작 사례연구. me.md/ai-direction.md "적정 복잡도" 신호 강화.
- **golf / 실행 습관 (있다면)**: "끝맺음의 순서" = 마무리 루틴에서 각 단계 완료 확인 후 다음으로 — 프로세스 규율 은유로 재사용 가능(선택).
- **projects/AISG·노지관리 (재사용 패턴)**: 2단계 graceful teardown(drain 검증 → 자원 유지 → ACK → 종료)은 무인 엣지 노드(전원·통신 자원 공유)의 안전 종료 표준 패턴. 정전/스케줄 종료 시 데이터 유실 0을 요구하는 관제 제품에 직결.
- **gaps (검증 성숙도)**: 5단계 판정 범례(CODE_DONE/E2E_PENDING/APP_PENDING)는 "구현 완료 ≠ 검증 완료"를 규율화한 사례 → 진척 보고/포트폴리오에서 과대표현 회피 역량 신호.

## §5. myWiki/entities/revita.md 갱신 권장 (한 줄)

> "2026-07 SBC 종료 2단계화(#23): `relay_stop`(신규 evt 0x09 — LTE TX ring drain 검증 후 relay OFF·**5V 유지**·`relay_stopped` ACK) → SBC가 MQTT·LTE 정리 → `shutdown`(실제 전원 OFF·resume). `sbc_module.c` cleanup 을 4함수로 분해(soft/force), `sbc_relay_drain()` bool화. `west build` OK=MCU **CODE_DONE**, TC-40 E2E는 HW 대기. 교훈: 종료는 원자적 사건이 아니라 순서 있는 프로토콜."

---

## 미처리 시 영향

흡수 누락 시 손실: ① **"종료=순서 있는 프로토콜" 교재 함정** — teardown 데이터 유실은 임베디드/분산 강의·포트폴리오 1급 자산인데 누락, ② **"적정 복잡도" 3부작**(생성·구조·소멸 라이프사이클)이 반쪽만 남아 사례연구 미완, ③ 2단계 graceful teardown이 노지관리/AISG 무인 노드 재사용 패턴으로 미연결, ④ 5단계 검증 범례(CODE_DONE 등)의 "구현≠검증" 규율 신호 소실, ⑤ revita.md가 #22 시점(단일 shutdown)으로 stale — 제품 종료 동작 서술 부정확.

## 처리 후 응답 형식

```
type: done
subject: ACK ingest #23 absorbed
관련 갱신: skills.md / gaps.md / thoughts/2026-07-03-{matching}.md / entities/revita.md
```

> 참고: 선행 카드 `2026-06-24-001`(#19) · `2026-06-30-001`(#20) · `2026-07-01-002`(#22) 아직 pending — #22/#23은 같은 SBC 통신 설계의 연속 서사(복귀→정제)라 **묶어 흡수 시 맥락 이득 최대**.
