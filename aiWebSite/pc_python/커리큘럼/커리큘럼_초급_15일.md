# Python(PC) 초급 과정 - 15일 커리큘럼

## 과정 개요

| 항목 | 내용 |
|------|------|
| **대상** | 프로그래밍 처음 입문자 |
| **기간** | 15일 (하루 30분~1시간) |
| **목표** | Python 기본 문법 이해 및 간단한 프로그램 작성 |
| **선수 조건** | VS Code, Python 설치 완료 |

---

## Day 1: Python 소개와 첫 프로그램

### 목표 1: Python의 특징과 활용 분야 이해하기
- Python을 만든 사람 (Guido van Rossum, 1991년)
- Python의 특징 (간결한 문법, 인터프리터 언어, 동적 타이핑)
- 활용 분야 (AI, 데이터 분석, 웹 개발, 자동화)

### 목표 2: 첫 번째 Hello World 프로그램 실행하기
- VS Code에서 .py 파일 만들기
- print() 함수 사용법
- 프로그램 실행 방법 (실행 버튼, 터미널)

---

## Day 2: 변수와 자료형

### 목표 1: 변수의 개념과 선언 방법 이해하기
- 변수란 무엇인가 (데이터를 저장하는 공간)
- Python에서 변수 만들기 (자료형 선언 불필요)
- 변수 이름 규칙 (시작 문자, 예약어)

### 목표 2: 기본 자료형 (숫자, 문자열, 불린)
- 정수(int)와 실수(float)
- 문자열(str) - 작은따옴표, 큰따옴표
- 불린(bool) - True, False
- type() 함수로 자료형 확인

### 목표 3: 사용자 입력 받기 (input 함수)
- input() 함수 기본 사용법
- input()은 항상 문자열 반환
- int(), float()로 형변환

---

## Day 3: 연산자

### 목표 1: 산술 연산자
- 기본 연산: +, -, *, /
- 나머지: %, 몫: //, 거듭제곱: **
- 연산 우선순위

### 목표 2: 비교 연산자와 논리 연산자
- 비교: ==, !=, <, >, <=, >=
- 논리: and, or, not
- 비교 연산의 결과는 True/False

---

## Day 4: 조건문 (if)

### 목표 1: if 문 기본 구조
- if 조건: 의 형태
- 들여쓰기의 중요성 (4칸 스페이스)
- 조건이 True일 때만 실행

### 목표 2: if-else와 if-elif-else
- else: 조건이 False일 때 실행
- elif: 여러 조건 검사
- 실습: 성적 등급 판정 프로그램

---

## Day 5: 반복문 (for)

### 목표 1: for 문과 range() 함수
- for 변수 in range(): 구조
- range(n): 0부터 n-1까지
- range(start, end): 시작과 끝 지정
- range(start, end, step): 증가값 지정

### 목표 2: for 문 활용 예제
- 1부터 10까지 합 구하기
- 구구단 출력하기
- 별(*) 찍기 패턴

---

## Day 6: 반복문 (while)

### 목표 1: while 문 기본 구조
- while 조건: 의 형태
- 조건이 True인 동안 반복
- 무한루프 주의

### 목표 2: break와 continue
- break: 반복문 즉시 종료
- continue: 현재 반복 건너뛰기
- 실습: 숫자 맞추기 게임

---

## Day 7: 문자열 다루기

### 목표 1: 문자열 인덱싱과 슬라이싱
- 인덱스: 0부터 시작, 음수 인덱스
- 슬라이싱: [start:end:step]
- 문자열은 불변(immutable)

### 목표 2: 문자열 메서드
- upper(), lower(), strip()
- split(), join()
- find(), replace()
- len() 함수

---

## Day 8: 리스트 (List) 기초

### 목표 1: 리스트 생성과 인덱싱
- 리스트란? (여러 값을 저장하는 자료형)
- 리스트 생성: [], list()
- 인덱싱과 슬라이싱

### 목표 2: 리스트 수정과 메서드
- 값 변경, 추가(append), 삽입(insert)
- 삭제: remove(), pop(), del
- 정렬: sort(), reverse()

---

## Day 9: 리스트 활용

### 목표 1: 리스트와 반복문
- for문으로 리스트 순회
- enumerate()로 인덱스와 함께 순회
- in 연산자로 포함 여부 확인

### 목표 2: 리스트 컴프리헨션
- [표현식 for 변수 in 리스트]
- 조건 추가: [표현식 for 변수 in 리스트 if 조건]
- 실습: 짝수만 필터링, 제곱 리스트 만들기

---

## Day 10: 튜플과 딕셔너리

### 목표 1: 튜플 (Tuple)
- 튜플이란? (변경 불가능한 리스트)
- 튜플 생성: (), tuple()
- 언패킹: a, b, c = (1, 2, 3)

### 목표 2: 딕셔너리 (Dictionary)
- 딕셔너리란? (키-값 쌍)
- 생성: {}, dict()
- 값 접근, 추가, 수정, 삭제
- keys(), values(), items()

---

## Day 11: 함수 기초

### 목표 1: 함수 정의와 호출
- def 함수이름(): 구조
- 함수를 사용하는 이유 (재사용, 가독성)
- return 문

### 목표 2: 매개변수와 인자
- 매개변수 vs 인자
- 여러 개의 매개변수
- 기본값 매개변수

---

## Day 12: 함수 심화

### 목표 1: 지역 변수와 전역 변수
- 지역 변수: 함수 안에서만 사용
- 전역 변수: 프로그램 전체에서 사용
- global 키워드

### 목표 2: 가변 인자
- *args: 여러 개의 인자
- **kwargs: 키워드 인자
- 실습: 합계 계산 함수 만들기

---

## Day 13: 모듈과 패키지

### 목표 1: 모듈 사용하기
- import 문
- from 모듈 import 함수
- as로 별칭 지정

### 목표 2: 주요 내장 모듈
- math: sqrt(), pow(), pi
- random: randint(), choice(), shuffle()
- datetime: 날짜와 시간 다루기

---

## Day 14: 파일 입출력

### 목표 1: 파일 읽기
- open() 함수와 모드 ('r', 'w', 'a')
- read(), readline(), readlines()
- with 문으로 안전하게 파일 다루기

### 목표 2: 파일 쓰기
- write(), writelines()
- 텍스트 파일 생성 및 수정
- 실습: 메모장 프로그램 만들기

---

## Day 15: 미니 프로젝트

### 목표 1: 프로젝트 기획
- 지금까지 배운 내용 복습
- 프로젝트 아이디어 선정
- 기능 목록 작성

### 목표 2: 프로젝트 구현
- 선택 프로젝트:
  - 가위바위보 게임
  - 간단한 계산기
  - 단어장 프로그램
  - 숫자 맞추기 게임
- 코드 작성 및 테스트

---

## 학습 성과

초급 과정을 완료하면:
- Python 기본 문법을 이해할 수 있다
- 변수, 조건문, 반복문을 활용할 수 있다
- 리스트, 딕셔너리 등 자료구조를 사용할 수 있다
- 함수를 정의하고 호출할 수 있다
- 파일을 읽고 쓸 수 있다
- 간단한 프로그램을 스스로 작성할 수 있다
