# Python(PC) 중급 과정 - 30일 커리큘럼

## 과정 개요

| 항목 | 내용 |
|------|------|
| **대상** | 초급 과정 완료자 또는 Python 기초 문법 이해자 |
| **기간** | 30일 (하루 30분~1시간) |
| **목표** | 객체지향 프로그래밍, 고급 자료구조, 실용적인 프로젝트 구현 |
| **선수 조건** | Python 초급 과정 완료 |

---

## PART 1: 고급 자료구조와 알고리즘 (Day 1~7)

### Day 1: 리스트 심화

#### 목표 1: 중첩 리스트 (2차원 리스트)
- 2차원 리스트 생성과 접근
- 행렬 표현하기
- 중첩 반복문으로 순회

#### 목표 2: 리스트 정렬 심화
- sort()와 sorted()의 차이
- key 매개변수 활용
- lambda를 이용한 정렬 기준 지정

---

### Day 2: 세트 (Set)

#### 목표 1: 세트 기본
- 세트란? (중복 없는 집합)
- 세트 생성: {}, set()
- 요소 추가(add), 삭제(remove, discard)

#### 목표 2: 집합 연산
- 합집합: |, union()
- 교집합: &, intersection()
- 차집합: -, difference()
- 대칭차집합: ^, symmetric_difference()

---

### Day 3: 딕셔너리 심화

#### 목표 1: 딕셔너리 고급 활용
- get() 메서드와 기본값
- setdefault()
- 딕셔너리 컴프리헨션

#### 목표 2: 중첩 딕셔너리
- 딕셔너리 안의 딕셔너리
- JSON 형태의 데이터 다루기
- 실습: 학생 성적 관리 시스템

---

### Day 4: 문자열 포맷팅

#### 목표 1: 다양한 포맷팅 방법
- % 연산자 (구식)
- format() 메서드
- f-string (권장)

#### 목표 2: 고급 포맷팅
- 정렬과 채우기
- 숫자 포맷 (소수점, 천단위)
- 날짜/시간 포맷

---

### Day 5: 정규표현식 기초

#### 목표 1: 정규표현식 개념
- 정규표현식이란?
- re 모듈 import
- match(), search(), findall()

#### 목표 2: 패턴 문법
- 메타문자: . ^ $ * + ? { } [ ] \ | ( )
- 문자 클래스: \d, \w, \s
- 실습: 이메일, 전화번호 검증

---

### Day 6: 정규표현식 활용

#### 목표 1: 그룹과 치환
- 그룹 캡처: ()
- sub()로 문자열 치환
- split()로 문자열 분리

#### 목표 2: 실전 활용
- 웹 페이지에서 URL 추출
- 로그 파일 파싱
- 데이터 정제(Cleaning)

---

### Day 7: 알고리즘 기초

#### 목표 1: 검색 알고리즘
- 선형 검색 (Linear Search)
- 이진 검색 (Binary Search)
- 시간 복잡도 개념

#### 목표 2: 정렬 알고리즘
- 버블 정렬 (Bubble Sort)
- 선택 정렬 (Selection Sort)
- 삽입 정렬 (Insertion Sort)

---

## PART 2: 객체지향 프로그래밍 (Day 8~14)

### Day 8: 클래스와 객체 기초

#### 목표 1: 클래스 정의
- 클래스란? (설계도)
- class 키워드
- __init__() 생성자

#### 목표 2: 객체 생성과 사용
- 인스턴스 생성
- 인스턴스 변수
- self의 의미

---

### Day 9: 메서드

#### 목표 1: 인스턴스 메서드
- 메서드 정의
- self 매개변수
- 메서드 호출

#### 목표 2: 클래스 변수와 메서드
- 클래스 변수 vs 인스턴스 변수
- @classmethod
- @staticmethod

---

### Day 10: 캡슐화

#### 목표 1: 접근 제어
- public, protected, private
- 언더스코어 규칙 (_, __)
- name mangling

#### 목표 2: getter와 setter
- @property 데코레이터
- 값 검증
- 읽기 전용 속성

---

### Day 11: 상속

#### 목표 1: 상속 기초
- 상속이란?
- 부모 클래스와 자식 클래스
- super() 함수

#### 목표 2: 메서드 오버라이딩
- 부모 메서드 재정의
- super()로 부모 메서드 호출
- 실습: 도형 클래스 계층 구조

---

### Day 12: 다형성과 추상 클래스

#### 목표 1: 다형성
- 다형성이란?
- 같은 이름, 다른 동작
- 덕 타이핑 (Duck Typing)

#### 목표 2: 추상 클래스
- abc 모듈
- @abstractmethod
- 추상 클래스를 사용하는 이유

---

### Day 13: 다중 상속과 믹스인

#### 목표 1: 다중 상속
- 여러 클래스 상속
- MRO (Method Resolution Order)
- super()의 동작 방식

#### 목표 2: 믹스인 패턴
- 믹스인이란?
- 기능 조합하기
- 실습: 로깅 믹스인

---

### Day 14: 특수 메서드 (매직 메서드)

#### 목표 1: 기본 특수 메서드
- __str__, __repr__
- __len__, __getitem__
- __eq__, __lt__, __gt__

#### 목표 2: 연산자 오버로딩
- __add__, __sub__, __mul__
- __contains__, __iter__
- 실습: 벡터 클래스 만들기

---

## PART 3: 예외 처리와 디버깅 (Day 15~17)

### Day 15: 예외 처리 기초

#### 목표 1: try-except 구문
- 예외란?
- try, except 블록
- 여러 예외 처리

#### 목표 2: 예외 종류
- SyntaxError, TypeError, ValueError
- IndexError, KeyError
- FileNotFoundError

---

### Day 16: 예외 처리 심화

#### 목표 1: else와 finally
- else: 예외가 발생하지 않았을 때
- finally: 항상 실행
- 자원 정리에 활용

#### 목표 2: 사용자 정의 예외
- Exception 클래스 상속
- raise 키워드
- 예외 메시지 커스터마이징

---

### Day 17: 디버깅

#### 목표 1: 디버깅 기법
- print() 디버깅
- assert 문
- 로깅 (logging 모듈)

#### 목표 2: VS Code 디버거
- 브레이크포인트 설정
- 변수 값 확인
- 스텝 오버, 스텝 인, 스텝 아웃

---

## PART 4: 함수형 프로그래밍 (Day 18~21)

### Day 18: 람다 함수

#### 목표 1: 람다 기초
- lambda 키워드
- 익명 함수
- 간단한 함수를 한 줄로

#### 목표 2: 람다 활용
- 정렬의 key로 사용
- 콜백 함수로 사용
- 즉석에서 함수 생성

---

### Day 19: map, filter, reduce

#### 목표 1: map()과 filter()
- map(): 모든 요소에 함수 적용
- filter(): 조건에 맞는 요소 필터링
- 리스트 컴프리헨션과 비교

#### 목표 2: reduce()
- functools.reduce()
- 누적 계산
- 실습: 합계, 곱, 최대값 구하기

---

### Day 20: 클로저와 데코레이터

#### 목표 1: 클로저
- 내부 함수
- 외부 변수 캡처
- 클로저 활용

#### 목표 2: 데코레이터
- 함수를 감싸는 함수
- @ 문법
- 실습: 실행 시간 측정 데코레이터

---

### Day 21: 제너레이터

#### 목표 1: 제너레이터 기초
- yield 키워드
- 제너레이터 함수
- 지연 평가 (Lazy Evaluation)

#### 목표 2: 제너레이터 표현식
- (표현식 for 변수 in 이터러블)
- 메모리 효율성
- 실습: 대용량 파일 처리

---

## PART 5: 실용 모듈과 라이브러리 (Day 22~27)

### Day 22: os와 sys 모듈

#### 목표 1: os 모듈
- 현재 디렉토리, 디렉토리 변경
- 파일/폴더 생성, 삭제
- 경로 다루기 (os.path)

#### 목표 2: sys 모듈
- 명령줄 인자 (sys.argv)
- 프로그램 종료 (sys.exit)
- Python 경로 (sys.path)

---

### Day 23: pathlib과 shutil

#### 목표 1: pathlib 모듈
- Path 객체
- 경로 조합 (/)
- 파일 존재 확인, 확장자 추출

#### 목표 2: shutil 모듈
- 파일 복사 (copy, copy2)
- 디렉토리 복사 (copytree)
- 파일/디렉토리 이동, 삭제

---

### Day 24: JSON과 CSV

#### 목표 1: JSON 다루기
- json 모듈
- loads(), dumps()
- load(), dump()

#### 목표 2: CSV 다루기
- csv 모듈
- reader, writer
- DictReader, DictWriter

---

### Day 25: 날짜와 시간

#### 목표 1: datetime 모듈
- date, time, datetime 객체
- 현재 날짜/시간 얻기
- 날짜 포맷팅 (strftime, strptime)

#### 목표 2: 날짜 계산
- timedelta로 날짜 연산
- 날짜 비교
- 실습: D-day 계산기

---

### Day 26: 웹 요청 (requests)

#### 목표 1: HTTP 기초
- HTTP 메서드 (GET, POST)
- 상태 코드 (200, 404, 500)
- requests 라이브러리 설치

#### 목표 2: API 호출
- GET 요청
- POST 요청
- JSON 응답 처리
- 실습: 날씨 API 호출

---

### Day 27: 웹 스크래핑 (BeautifulSoup)

#### 목표 1: HTML 파싱
- BeautifulSoup 설치
- HTML 구조 이해
- 태그 선택 (find, find_all)

#### 목표 2: 데이터 추출
- CSS 선택자 사용
- 텍스트, 속성 추출
- 실습: 뉴스 헤드라인 수집

---

## PART 6: 프로젝트 (Day 28~30)

### Day 28: 프로젝트 기획

#### 목표 1: 프로젝트 선정
- 프로젝트 아이디어 브레인스토밍
- 요구사항 정의
- 기능 목록 작성

#### 목표 2: 설계
- 클래스 설계
- 모듈 구조
- 데이터 흐름

---

### Day 29: 프로젝트 구현

#### 목표 1: 핵심 기능 구현
선택 프로젝트:
- 주소록 관리 프로그램
- 가계부 프로그램
- 할일 관리 (To-Do) 프로그램
- 웹 스크래핑 뉴스 수집기

#### 목표 2: 부가 기능 구현
- 파일 저장/불러오기
- 예외 처리
- 사용자 인터페이스 개선

---

### Day 30: 프로젝트 완성 및 회고

#### 목표 1: 테스트와 디버깅
- 다양한 입력으로 테스트
- 버그 수정
- 코드 정리

#### 목표 2: 회고
- 배운 내용 정리
- 개선할 점 기록
- 다음 단계 계획

---

## 학습 성과

중급 과정을 완료하면:
- 객체지향 프로그래밍을 이해하고 적용할 수 있다
- 클래스, 상속, 다형성을 활용할 수 있다
- 예외 처리로 안정적인 프로그램을 작성할 수 있다
- 람다, 데코레이터, 제너레이터를 사용할 수 있다
- 파일, JSON, CSV 데이터를 다룰 수 있다
- 웹 API 호출과 스크래핑을 할 수 있다
- 중간 규모의 프로젝트를 설계하고 구현할 수 있다
