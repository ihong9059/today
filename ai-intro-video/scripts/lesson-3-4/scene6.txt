PyTorch에서 순전파를 구현해 봅시다.
먼저 신경망 클래스를 정의합니다.
nn.Module을 상속받아 만듭니다.
init 메서드에서 레이어들을 정의합니다.
nn.Linear로 선형 레이어를 만듭니다.
forward 메서드에서 순전파를 정의합니다.
x가 들어오면 첫 번째 레이어를 통과합니다.
ReLU 활성화 함수를 적용합니다.
두 번째 레이어를 통과해 최종 출력을 반환합니다.
모델에 입력을 넣으면 자동으로 forward가 실행됩니다.