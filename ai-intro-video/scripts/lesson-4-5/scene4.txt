transforms는 데이터 전처리를 담당합니다.

이미지 데이터의 경우 다양한 변환이 필요합니다.
ToTensor로 이미지를 텐서로 변환합니다.
Normalize로 픽셀 값을 정규화합니다.
Resize로 이미지 크기를 조정합니다.

Compose를 사용하면 여러 변환을 순서대로 적용할 수 있습니다.
transforms.Compose에 변환 리스트를 전달하면 됩니다.
