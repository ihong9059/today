역전파의 핵심 단계를 정리합니다.
1단계: 순전파로 예측값을 계산합니다.
2단계: 손실을 계산합니다.
3단계: 출력층에서 기울기를 계산합니다.
4단계: 연쇄법칙으로 이전 레이어로 기울기를 전파합니다.
5단계: 모든 가중치의 기울기를 구합니다.
6단계: 경사하강법으로 가중치를 업데이트합니다.
이 과정이 매 학습 단계마다 반복됩니다.
PyTorch에서는 loss.backward()가 이 모든 것을 자동으로 합니다.