forward 메서드는 순전파를 정의합니다.

입력 x가 들어오면 어떻게 처리할지 순서대로 작성합니다.
x를 fc1에 통과시키고, ReLU를 적용하고,
fc2에 통과시키고, 다시 ReLU를 적용하고,
마지막으로 fc3에 통과시켜 출력합니다.

주의할 점이 있습니다.
모델을 호출할 때는 model.forward(x)가 아니라 model(x)를 사용합니다.
model(x)는 내부적으로 forward 외에도 hook 같은 추가 기능을 처리합니다.
