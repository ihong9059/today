nn.Sequential을 사용하면 레이어를 순서대로 쌓을 수 있습니다.

nn.Sequential에 레이어들을 순서대로 전달하면,
forward에서 자동으로 순서대로 실행됩니다.

간단한 모델은 Sequential로 깔끔하게 정의할 수 있습니다.
Linear, ReLU, Linear, ReLU, Linear처럼 쭉 나열하면 됩니다.

복잡한 분기가 필요하면 직접 forward를 작성하고,
단순한 순차 연결이면 Sequential이 편리합니다.
