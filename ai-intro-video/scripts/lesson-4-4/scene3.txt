커스텀 모델을 만들 때는 __init__ 메서드에서 레이어를 정의합니다.

반드시 super().__init__()를 먼저 호출해야 합니다.
이 호출이 없으면 nn.Module의 초기화가 되지 않아 오류가 발생합니다.

그 다음 self.fc1, self.fc2처럼 레이어를 정의합니다.
self에 할당된 레이어들은 자동으로 모델의 파라미터로 등록됩니다.

예를 들어 MLP 모델은 fc1, fc2, fc3 세 개의 Linear 레이어를 가질 수 있습니다.
