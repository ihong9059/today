이번 강의에서 nn.Module의 기초를 배웠습니다.

정리하면,
첫째, 커스텀 모델은 nn.Module을 상속받습니다.
둘째, __init__에서 super().__init__()을 호출하고 레이어를 정의합니다.
셋째, forward 메서드에서 순전파를 구현합니다.
넷째, parameters()로 파라미터를 관리합니다.

다음 강의에서는 모델 저장과 로드에 대해 알아보겠습니다.
