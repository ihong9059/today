nn.Module은 신경망 모듈의 최상위 부모 클래스입니다.

PyTorch의 모든 레이어는 nn.Module을 상속받아 만들어집니다.
nn.Linear는 nn.Module을 상속하고,
nn.Conv2d도 nn.Module을 상속합니다.

우리가 만드는 커스텀 모델도 nn.Module을 상속받아야 합니다.
class SimpleModel(nn.Module)처럼 선언하면 됩니다.

nn.Module을 상속받으면 파라미터 관리, GPU 이동, 저장과 로드 같은 기능을 자동으로 사용할 수 있습니다.
