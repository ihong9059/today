구체적인 계산 예시를 볼까요?

5곱하기5 이미지에 3곱하기3 커널을 적용한다고 가정해봅시다.
커널의 각 값과 이미지의 대응하는 값을 곱합니다.

예를 들어, 커널이 마이너스1, 0, 1 이런 식으로 구성되어 있다면,
이미지의 값들과 각각 곱해서 모두 더합니다.

결과값이 크면 해당 위치에 커널이 찾는 특징이 있다는 뜻이고,
작거나 0에 가까우면 그 특징이 없다는 의미입니다.