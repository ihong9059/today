실제 PyTorch 학습 코드를 보겠습니다.
지금까지 배운 모든 것이 합쳐진 코드입니다.

for epoch in range(epochs) 콜론.
    model.train()

    for batch_idx, (data, target) in enumerate(train_loader) 콜론.
        optimizer.zero_grad()
        output = model(data)
        loss = criterion(output, target)
        loss.backward()
        optimizer.step()

이중 반복문으로 epoch와 batch를 처리합니다.

그리고 검증 후 Early Stopping을 적용합니다.

if val_loss < best_val_loss 콜론.
    best_val_loss = val_loss
    torch.save(model.state_dict(), 'best_model.pt')
else 콜론.
    patience_counter += 1

if patience_counter >= max_patience 콜론.
    print("Early stopping")
    break

이것이 실제 딥러닝 학습의 표준 패턴입니다.
