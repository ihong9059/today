검증 루프도 중요합니다.

학습 중간에 모델 성능을 평가해야 합니다.
검증 데이터로 과적합을 확인합니다.

검증 시에는 model.eval()을 호출합니다.
드롭아웃과 배치 정규화가 평가 모드로 전환됩니다.

with torch.no_grad() 블록 안에서 수행합니다.
기울기 계산을 생략해 메모리를 절약합니다.

검증이 끝나면 model.train()으로 다시 학습 모드로 돌아갑니다.
