역전파 단계입니다. 가장 중요한 세 줄의 코드가 있습니다.

첫째, optimizer.zero_grad()로 기울기를 초기화합니다.
이전 배치의 기울기가 누적되지 않도록 합니다.

둘째, loss.backward()로 역전파를 수행합니다.
손실에 대한 각 파라미터의 기울기를 계산합니다.

셋째, optimizer.step()으로 가중치를 업데이트합니다.
계산된 기울기를 사용해 파라미터를 조정합니다.

이 세 줄이 PyTorch 학습의 핵심입니다.
