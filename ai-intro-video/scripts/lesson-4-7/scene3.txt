전체 모델 저장 방식도 있습니다.

토치 점 세이브에 모델 자체를 넣으면 모델 구조와 가중치가 함께 저장됩니다.

불러올 때는 토치 점 로드만 하면 바로 사용할 수 있어서 편리합니다.

하지만 주의할 점이 있습니다. 피클 방식으로 저장되기 때문에 모델 클래스 정의가 반드시 필요하고, 파이썬 버전이 다르면 호환 문제가 생길 수 있습니다.

따라서 빠른 테스트용으로는 전체 저장이 편하지만, 배포나 장기 보관에는 state_dict 방식을 쓰는 것이 안전합니다.
