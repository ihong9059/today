GPU와 CPU 간 모델 이동도 알아둬야 합니다.

GPU로 학습한 모델을 CPU에서 불러오려면 map_location 옵션을 씁니다. 토치 점 로드에 map_location 이퀄 씨피유를 추가하면 됩니다.

반대로 CPU에서 저장한 모델을 GPU에서 쓰려면 로드 후 모델 점 투 괄호 디바이스 괄호를 호출합니다.

멀티 GPU로 학습한 DataParallel 모델은 state_dict 키에 모듈 점이 붙어 있어서, 싱글 GPU에서 로드할 때 키 이름을 수정해야 할 수도 있습니다.
