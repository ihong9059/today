실제 학습에서는 체크포인트 저장이 필수입니다.

체크포인트에는 모델 가중치뿐 아니라 옵티마이저 상태, 현재 에폭, 손실 값 등을 함께 저장합니다.

이렇게 하면 학습이 중단되어도 정확히 그 지점부터 재개할 수 있습니다.

저장할 때는 딕셔너리에 에폭, model_state_dict, optimizer_state_dict, 로스를 담아서 토치 점 세이브합니다.

불러올 때는 체크포인트를 로드하고, 모델과 옵티마이저 각각에 load_state_dict를 호출합니다. 에폭 번호도 복원해서 이어서 학습하면 됩니다.
