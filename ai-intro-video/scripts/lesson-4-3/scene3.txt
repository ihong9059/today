그런데 단순히 정수만 쓰면 문제가 있습니다.

원핫 인코딩은 벡터가 너무 크고, 단어 간 관계를 표현하지 못합니다.

"영화"와 "시네마"가 완전히 다른 벡터가 되어버리죠.

그래서 단어 임베딩을 사용합니다.

각 단어를 128차원 정도의 밀집 벡터로 표현합니다.

비슷한 의미의 단어는 비슷한 벡터를 갖게 됩니다.

PyTorch에서는 nn.Embedding으로 임베딩 층을 만듭니다.