# 01. Prophet — "누구나 쓸 수 있는" 시계열 예측

## 이 시뮬레이션이 보여주는 것

| 항목 | 내용 |
|------|------|
| **모델** | Prophet (Meta/Facebook, 2017) |
| **시나리오** | 슈레더 베어링 온도 90일 데이터로 미래 7일 예측 |
| **핵심 체험** | 코드 3줄 예측, 트렌드/계절성 자동 분리, 불확실성 구간 |

## 실행 방법

```bash
pip install prophet pandas numpy matplotlib
cd 01_Prophet
python simulation.py
```

## Prophet이 하는 일

```
y(t) = g(t) + s(t) + h(t) + ε(t)
       트렌드  계절성  휴일   노이즈

이 시뮬레이션에서:
  g(t) = 칼날 마모에 의한 온도 상승 트렌드 (하루 +0.03°C)
  s(t) = 일간 패턴 (낮 가동→온도↑, 밤 정지→온도↓)
       + 주간 패턴 (주말 비가동→온도↓)
  ε(t) = 센서 노이즈 + 이상 고온 이벤트
```

## 결과 해석

1. **예측 그래프**: 파란점(실제) vs 빨간선(예측) + 분홍 영역(불확실성)
2. **트렌드**: 90일간 온도가 조금씩 상승 → 칼날 마모 징후
3. **일간 패턴**: 오전~오후에 높고, 야간에 낮음 → 가동 패턴 반영

## 핵심 교훈

- Prophet은 **가장 쉽게** 시계열 예측을 시작할 수 있는 모델
- **트렌드/계절성 분리** 기능이 강력 → "왜 올라가는지" 설명 가능
- 단, **단변량만 가능** → 온도+전류+진동을 함께 쓸 수 없음
