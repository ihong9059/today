먼저 NumPy 배열을 만들어볼까요?
np.array로 파이썬 리스트를 배열로 바꿀 수 있습니다.
np.zeros는 0으로 가득 찬 배열을, np.ones는 1로 가득 찬 배열을 만들어요.
np.arange는 연속된 숫자를 만듭니다.
파이썬 리스트와 다른 점은 뭘까요?
NumPy 배열은 전체에 한번에 연산이 가능합니다.
배열 곱하기 2를 하면, 모든 원소가 2배가 돼요.
리스트는 반복되지만, NumPy는 각 원소가 계산됩니다.
이것이 AI에서 수백만 개의 숫자를 빠르게 처리하는 비결입니다!