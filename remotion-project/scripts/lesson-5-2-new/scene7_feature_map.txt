합성곱의 결과물을 특성 맵이라고 합니다.
하나의 커널은 하나의 특성 맵을 만들어요.
수직 엣지 커널은 세로선을 감지하고, 수평 엣지 커널은 가로선을 감지합니다.
CNN에서는 여러 개의 커널을 동시에 사용해서, 다양한 특성을 추출해요.
파이토치의 Conv2d에서 아웃 채널 64라고 하면, 64개의 커널을 사용하는 겁니다.
각 커널은 학습을 통해 자동으로 최적의 값을 찾아갑니다.