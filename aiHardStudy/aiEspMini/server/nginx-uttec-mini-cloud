# uttec-ai.duckdns.org 기존 설정에 추가할 location 블록
# /mini → 8098 (UTTEC Mini Web UI)
# /mini/api/v1/ → 8098 → 8096 (Build API)

    # UTTEC Mini Web UI (포트 8098) — ESP32-C3 SuperMini
    location /mini {
        proxy_pass http://localhost:8098;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Mini 빌드 API 프록시 (포트 8098 → 8096)
    location /mini/api/v1/ {
        proxy_pass http://localhost:8098/api/v1/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
