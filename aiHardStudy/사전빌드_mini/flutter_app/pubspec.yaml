name: vibe_ut_cloud
description: UTTEC Cloud - AI coding to ESP32 via BLE OTA (Cloud Server)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_blue_plus: ^1.35.0
  permission_handler: ^11.3.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
