# 자격시험 가이드 사이트 작업 가이드

## 프로젝트 개요
- **사이트 URL**: https://uttec-cert.duckdns.org/
- **EC2 서버**: 3.36.91.83
- **SSH 키**: `~/.ssh/uttec-first-ec2.pem`
- **프로젝트 경로**: `/home/ec2-user/cert-guide/`
- **기술 스택**: Next.js 16.1.1 (Turbopack), PM2

---

## 핵심 작업: Study 페이지 패턴 통일

### 올바른 패턴 (기계기사 재료역학 패턴)

**핵심 특징:**
1. `expandedTopics` 상태 사용 (주제별 확장/축소)
2. 주제 클릭 시 해당 주제의 **모든 문제가 즉시 표시**
3. **정답이 문제 아래에 바로 표시** ("정답 보기" 버튼 없음)
4. **3개 AI 버튼이 바로 보임** (Claude, ChatGPT, Gemini)
5. 진도율 표시 포함

**상태 관리 코드:**
```tsx
const [completedQuestions, setCompletedQuestions] = useState<Record<string, boolean>>({});
const [expandedTopics, setExpandedTopics] = useState<Record<string, boolean>>({});

const toggleTopic = (topicId: string) => {
  setExpandedTopics(prev => ({ ...prev, [topicId]: !prev[topicId] }));
};
```

**문제 표시 구조:**
```tsx
{expandedTopics[topic.id] && (
  <div className="p-4 space-y-4">
    {topic.questions.map((q) => {
      const key = `${topic.id}-${q.id}`;
      const isCompleted = completedQuestions[key];
      return (
        <div key={q.id} className={`p-4 rounded-lg border ${isCompleted ? 'bg-green-50 border-green-200' : 'bg-gray-50 border-gray-200'}`}>
          {/* 문제 번호와 질문 */}
          <div className="flex items-start gap-3 mb-3">
            <span className={`w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold ${isCompleted ? 'bg-green-500 text-white' : 'bg-gray-300 text-gray-600'}`}>
              {isCompleted ? '✓' : q.id}
            </span>
            <p className="flex-1 text-gray-800 font-medium">{q.question}</p>
          </div>
          {/* 정답 바로 표시 */}
          <p className="text-sm text-gray-600 mb-3"><strong>정답:</strong> {q.answer}</p>
          {/* AI 버튼 3개 바로 표시 */}
          <div className="flex gap-2 flex-wrap">
            <a href={`https://claude.ai/new?q=${encodeURIComponent(q.prompt)}`} ...>🧡 Claude</a>
            <a href={`https://chat.openai.com/?q=${encodeURIComponent(q.prompt)}`} ...>💚 ChatGPT</a>
            <a href={`https://gemini.google.com/app?q=${encodeURIComponent(q.prompt)}`} ...>💙 Gemini</a>
            <button onClick={() => toggleComplete(topic.id, q.id)} ...>✓ 완료</button>
          </div>
        </div>
      );
    })}
  </div>
)}
```

### 잘못된 패턴 (수정 대상)

**잘못된 특징:**
1. `activeTopicId`, `activeQuestionId`, `showAnswer` 상태 사용
2. 한 번에 **한 문제만 표시**
3. **"정답 보기" 버튼 클릭 필요**
4. AI 버튼이 정답 보기 후에만 표시

**잘못된 상태 관리:**
```tsx
// 이런 패턴은 수정 필요!
const [activeTopicId, setActiveTopicId] = useState(topics[0].id);
const [activeQuestionId, setActiveQuestionId] = useState(1);
const [showAnswer, setShowAnswer] = useState(false);
```

---

## 작업 절차

### 1단계: EC2에서 기존 페이지 확인
```bash
# topics 데이터 가져오기
ssh -i ~/.ssh/uttec-first-ec2.pem ec2-user@3.36.91.83 "grep -A 500 'const topics' /home/ec2-user/cert-guide/app/category/[분야]/[자격증]/study/[과목]/page.tsx | head -200"
```

### 2단계: 로컬에서 새 파일 생성
- 기존 topics 데이터를 그대로 사용
- 컴포넌트 구조만 올바른 패턴으로 변경
- 파일 저장 위치: `C:/todo/today/자격시험/[파일명]-new.tsx`

### 3단계: EC2에 업로드
```bash
scp -i ~/.ssh/uttec-first-ec2.pem "C:/todo/today/자격시험/[파일명]-new.tsx" ec2-user@3.36.91.83:/home/ec2-user/cert-guide/app/category/[분야]/[자격증]/study/[과목]/page.tsx
```

### 4단계: 빌드 및 배포
```bash
# 빌드
ssh -i ~/.ssh/uttec-first-ec2.pem ec2-user@3.36.91.83 "cd /home/ec2-user/cert-guide && npm run build 2>&1"

# PM2 재시작
ssh -i ~/.ssh/uttec-first-ec2.pem ec2-user@3.36.91.83 "pm2 restart cert-guide && pm2 status"
```

---

## 디렉토리 구조

```
/home/ec2-user/cert-guide/app/category/
├── mechanical/              # 기계·제어 분야 (완료)
│   ├── mechanical-engineer/     # 기계기사 (완료)
│   ├── mechanical-craftsman/    # 기계산업기사 (완료)
│   ├── welding-engineer/        # 용접기사 (완료)
│   ├── control-engineer/        # 제어기사 (완료)
│   ├── electric-engineer/       # 전기기사 (완료)
│   └── electric-craftsman/      # 전기산업기사 (완료)
├── electrical/              # 전기·전자 분야
├── electronic/              # 전자 분야
└── [추가 분야]/             # 화학·환경 등 추가 예정
```

---

## 완료된 작업 목록 (2026-01-03 기준)

### 기계·제어 분야
| 자격증 | 과목 | 상태 |
|--------|------|------|
| 기계기사 | 재료역학, 유체역학, 열역학, 기계설계, 기계제작, 실기 | ✅ 완료 |
| 기계산업기사 | 재료, 설계, 제도, 가공, 실기 | ✅ 완료 |
| 용접기사 | 용접야금, 용접구조설계, 용접시공, 용접검사, 실기 | ✅ 완료 |
| 제어기사 | 제어공학, 전자기기, 계측기기, 공정제어, 실기 | ✅ 완료 |
| 전기기사 | 전기자기학, 전력공학, 전기기기, 회로제어, 설계기준, 실기 | ✅ 완료 |
| 전기산업기사 | 전기자기학, 전력공학, 전기기기, 회로이론, 실기 | ✅ 완료 |

---

## 주의사항

1. **topics 데이터는 그대로 유지**: 문제/정답/프롬프트 데이터는 변경하지 않음
2. **localStorage 키 유지**: 기존 진도 데이터 호환성 유지
3. **빌드 전 확인**: 업로드 후 반드시 `npm run build` 성공 확인
4. **PM2 재시작 필수**: 빌드 후 `pm2 restart cert-guide` 실행

---

## 참고 파일

완성된 패턴 예시:
- `C:/todo/today/자격시험/welding-metallurgy-new.tsx` (용접야금)
- `C:/todo/today/자격시험/electromagnetics-new.tsx` (전기자기학)
- `C:/todo/today/자격시험/power-new.tsx` (전력공학)

이 파일들을 템플릿으로 사용하여 새로운 분야/자격증 페이지 작성 가능
