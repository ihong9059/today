# 자격시험 가이드 웹사이트 작업 지시서

## 개요
이 문서는 자격시험 가이드 웹사이트에 새로운 분야를 추가하기 위한 작업 지시서입니다.

## 프로젝트 정보

### 서버 정보
- **EC2 IP**: 3.36.91.83
- **SSH 접속**: `ssh -i ~/.ssh/uttec-first-ec2.pem ec2-user@3.36.91.83`
- **프로젝트 경로**: `/home/ec2-user/cert-guide/`
- **URL**: https://uttec-cert.duckdns.org

### 기술 스택
- Next.js 16.1.1 (Turbopack)
- TypeScript
- Tailwind CSS
- PM2 (프로세스 매니저)

---

## 현재 완료된 분야

| 분야 | 카테고리 ID | 자격증 수 |
|------|------------|----------|
| 기계·전기·전자 | mechanical | 6개 |
| 화학·환경 | chemistry | 5개 |

---

## 작업 패턴 (기계기사 패턴)

### 1. 파일 구조
```
C:\todo\today\자격시험\{category-id}\
├── category-page.tsx          # 카테고리 메인 페이지
├── {cert-id}\
│   ├── main-page.tsx          # 자격증 메인 페이지
│   ├── exam-page.tsx          # 시험 정보 페이지
│   └── study\
│       ├── {subject-1}.tsx    # 과목별 학습 페이지
│       ├── {subject-2}.tsx
│       └── practical.tsx      # 실기 페이지
```

### 2. Study 페이지 핵심 패턴

```tsx
'use client';
import { useState, useEffect } from 'react';
import Link from 'next/link';

interface Question { id: string; question: string; options: string[]; answer: number; explanation: string; }
interface Topic { id: string; title: string; description: string; icon: string; questions: Question[]; }

export default function SubjectPage() {
  const [completedQuestions, setCompletedQuestions] = useState<Record<string, boolean>>({});
  const [expandedTopics, setExpandedTopics] = useState<Record<string, boolean>>({});
  const subject = { id: 'subject-id', name: '과목명', icon: '📚', color: 'from-blue-500 to-cyan-500' };

  const topics: Topic[] = [
    { id: 'topic-1', title: '토픽 제목', description: '토픽 설명', icon: '📖',
      questions: [
        { id: 'q-1', question: '문제 내용?', options: ['선택지1', '선택지2', '선택지3', '선택지4'], answer: 0, explanation: '해설 내용' },
        // ... 5개 문제
      ]},
    // ... 3-5개 토픽
  ];

  // localStorage 기반 진행률 저장
  useEffect(() => {
    const saved = localStorage.getItem(`completed-${subject.id}`);
    if (saved) setCompletedQuestions(JSON.parse(saved));
  }, []);

  // 문제 완료 토글
  const toggleQuestion = (qId: string) => {
    const n = { ...completedQuestions, [qId]: !completedQuestions[qId] };
    setCompletedQuestions(n);
    localStorage.setItem(`completed-${subject.id}`, JSON.stringify(n));
  };

  // 토픽 확장 토글
  const toggleTopic = (tId: string) => setExpandedTopics(p => ({ ...p, [tId]: !p[tId] }));

  // AI 버튼용 프롬프트 생성
  const prompt = (q: Question) => `[자격증명 - 과목명] 문제:\n${q.question}\n\n선택지:\n${q.options.map((o,i)=>`${i+1}. ${o}`).join('\n')}\n\n정답: ${q.answer+1}번 (${q.options[q.answer]})\n해설: ${q.explanation}\n\n이 문제에 대해 더 자세히 설명해주세요.`;

  // ... JSX 렌더링
}
```

### 3. 핵심 UI 특징
- **expandedTopics**: 아코디언 스타일 토픽 확장
- **정답 즉시 표시**: "정답 보기" 버튼 없이 바로 표시
- **3개 AI 버튼**: Claude, ChatGPT, Gemini 버튼 항상 표시
- **진행률 추적**: localStorage 기반 체크박스

---

## 새 분야 추가 작업 지시 예시

### Claude에게 보낼 프롬프트

```
자격시험 가이드 웹사이트에 새로운 분야를 추가해주세요.

## 프로젝트 정보
- EC2: 3.36.91.83
- SSH: ssh -i ~/.ssh/uttec-first-ec2.pem ec2-user@3.36.91.83
- 경로: /home/ec2-user/cert-guide/
- URL: https://uttec-cert.duckdns.org

## 작업할 분야
- 분야명: [예: 안전·소방]
- 카테고리 ID: [예: safety]
- 자격증 목록:
  1. 산업안전기사 (industrial-safety)
  2. 소방설비기사(기계) (fire-equipment-mechanical)
  3. 위험물기능장 (hazardous-master)
  ...

## 작업 패턴
기존 "화학·환경(chemistry)" 분야와 동일한 패턴으로 작업해주세요:
- C:\todo\today\자격시험\{category-id}\ 폴더에 파일 생성
- 각 자격증별 main-page.tsx, exam-page.tsx, study/*.tsx 생성
- study 페이지는 "기계기사 패턴" 사용:
  - expandedTopics 상태로 토픽 아코디언
  - 정답 즉시 표시 (정답 보기 버튼 없음)
  - Claude, ChatGPT, Gemini 3개 AI 버튼 항상 표시
  - localStorage 기반 진행률 저장

## 완료 후
1. EC2에 파일 복사: scp -i ~/.ssh/uttec-first-ec2.pem -r /c/todo/today/자격시험/{category-id} ec2-user@3.36.91.83:/home/ec2-user/cert-guide/app/category/
2. page.tsx 파일 생성 (Next.js 라우팅용)
3. npm run build && pm2 restart cert-guide
4. 홈페이지(app/page.tsx)에 해당 분야 활성화
```

---

## EC2 배포 명령어 요약

### 1. 파일 복사
```bash
scp -i ~/.ssh/uttec-first-ec2.pem -r /c/todo/today/자격시험/{category-id} ec2-user@3.36.91.83:/home/ec2-user/cert-guide/app/category/
```

### 2. page.tsx 생성 (SSH 접속 후)
```bash
# 카테고리 페이지
echo "export { default } from './category-page';" > app/category/{category-id}/page.tsx

# 자격증 메인 페이지
echo "export { default } from './main-page';" > app/category/{category-id}/{cert-id}/page.tsx

# exam 페이지
mkdir -p app/category/{category-id}/{cert-id}/exam
echo "export { default } from '../exam-page';" > app/category/{category-id}/{cert-id}/exam/page.tsx

# study 페이지들
mkdir -p app/category/{category-id}/{cert-id}/study/{subject-id}
echo "export { default } from '../../study/{subject-id}';" > app/category/{category-id}/{cert-id}/study/{subject-id}/page.tsx
```

### 3. 빌드 및 재시작
```bash
cd /home/ec2-user/cert-guide
npm run build
pm2 restart cert-guide
```

### 4. 홈페이지 수정
`app/page.tsx`에서:
1. categories 배열에 `hasPage: true, href: '/category/{category-id}'` 추가
2. "지금 학습 가능한 분야" 섹션에 Link 카드 추가

---

## 참고: 색상 팔레트

| 분야 | 색상 |
|------|------|
| 기계 | from-orange-500 to-amber-500 |
| 전기 | from-yellow-500 to-orange-500 |
| 전자 | from-blue-500 to-indigo-500 |
| 화학·환경 | from-green-500 to-emerald-600 |
| 안전·소방 | from-red-500 to-orange-500 |
| IT | from-blue-500 to-cyan-500 |
| 건축 | from-orange-500 to-red-500 |

---

## 체크리스트

- [ ] 카테고리 페이지 생성 (category-page.tsx)
- [ ] 각 자격증 메인 페이지 생성 (main-page.tsx)
- [ ] 각 자격증 시험정보 페이지 생성 (exam-page.tsx)
- [ ] 각 과목별 study 페이지 생성 (study/*.tsx)
- [ ] EC2에 파일 복사
- [ ] Next.js 라우팅용 page.tsx 파일 생성
- [ ] npm run build 성공 확인
- [ ] pm2 restart 완료
- [ ] 홈페이지에 분야 활성화
- [ ] 웹사이트에서 동작 확인
