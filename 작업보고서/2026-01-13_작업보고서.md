# 2026-01-13 작업 보고서

## Sensor Monitor 앱 개선

### 작업 목표
- 스마트폰 모델명 앱 헤더에 동적 표시 (여러 대 구분용)
- Galaxy A23 카메라 문제 해결

---

## 완료된 작업

### 1. 디바이스 모델명 동적 표시 기능 추가

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| **디바이스명** | 하드코딩 "Galaxy M53 5G" | `Build.MANUFACTURER + Build.MODEL` 동적 생성 |
| **앱 헤더** | 디바이스명 미표시 | 모델명 표시 |
| **서버 전송** | 고정 디바이스명 | 실제 디바이스명 |

#### 코드 변경 내용 (MainActivity.kt)

```kotlin
// 추가된 import
import android.os.Build

// 디바이스명 프로퍼티 추가
private val deviceName: String by lazy { "${Build.MANUFACTURER} ${Build.MODEL}" }

// 헤더 TextView 연결
private lateinit var deviceModelText: TextView
private lateinit var headerServerStatusText: TextView

// onCreate에서 초기화
deviceModelText = findViewById(R.id.deviceModelText)
headerServerStatusText = findViewById(R.id.serverStatusText)
deviceModelText.text = "Device: $deviceName"

// 서버 전송 시 동적 디바이스명 사용
json.put("device", deviceName)  // 기존: "Galaxy M53 5G"
```

### 2. Galaxy A23 카메라 YUV 변환 오류 수정

#### 문제 원인
```
YUV conversion failed: off=0, len=491136 out of bounds (size=460800)
```
- Y plane의 row stride(768)가 이미지 width(640)보다 큼
- `yBuffer.get(nv21, 0, ySize)`에서 ySize(491136)가 nv21 배열 크기(460800)를 초과
- Galaxy A23의 카메라 버퍼 레이아웃이 M53과 다름

#### 해결 방법 (MainActivity.kt:449-466)

**변경 전:**
```kotlin
val ySize = yBuffer.remaining()
// ...
yBuffer.get(nv21, 0, ySize)  // 오류 발생
```

**변경 후:**
```kotlin
val yRowStride = yPlane.rowStride

// Copy Y plane row by row (handles row stride > width)
if (yRowStride == width) {
    // Fast path: no padding, copy all at once
    yBuffer.get(nv21, 0, width * height)
} else {
    // Slow path: copy row by row to skip padding
    for (row in 0 until height) {
        yBuffer.position(row * yRowStride)
        yBuffer.get(nv21, row * width, width)
    }
}
```

### 3. APK 빌드 및 배포

| 작업 | 상태 |
|------|------|
| APK 빌드 | 완료 |
| Galaxy A23 설치 | 완료 |
| Galaxy A23 카메라 동작 확인 | 완료 |
| Galaxy M53 설치 | **대기** (USB 연결 필요) |

---

## 현재 서버 등록 디바이스

| 디바이스 | 모델명 | 상태 |
|----------|--------|------|
| Galaxy M53 5G | Galaxy M53 5G | 연결 중 (구버전 앱) |
| Galaxy A23 | samsung SM-A235N | 연결 중 (신버전 앱) |

---

## 프로그램 위치

### 로컬 (Windows)

| 항목 | 경로 |
|------|------|
| **Android 프로젝트** | `C:\todo\today\SensorMonitor\` |
| **MainActivity.kt** | `C:\todo\today\SensorMonitor\app\src\main\java\com\example\sensormonitor\MainActivity.kt` |
| **activity_main.xml** | `C:\todo\today\SensorMonitor\app\src\main\res\layout\activity_main.xml` |
| **빌드된 APK** | `C:\todo\today\SensorMonitor\app\build\outputs\apk\debug\app-debug.apk` |

### EC2 서버

| 항목 | 값 |
|------|-----|
| **IP** | 52.78.119.132 |
| **대시보드 URL** | http://52.78.119.132:5000 |
| **서버 코드** | `~/sensor-monitor/server.js` |
| **웹 대시보드** | `~/sensor-monitor/public/index.html` |

---

## 다음 세션 작업

1. **Galaxy M53에 업데이트된 APK 설치**
   - USB 연결 후 adb install 실행
   - 앱 헤더에 디바이스명 표시 확인

2. **양쪽 디바이스 카메라 동시 모니터링 테스트**
   - 웹 대시보드에서 탭 전환하며 확인

3. **디바이스명 표시 형식 개선 (선택)**
   - 현재: "samsung SM-A235N"
   - 개선: "Galaxy A23" (친숙한 이름으로)

---

## APK 설치 명령어 (다음 세션용)

```bash
# 연결된 디바이스 확인
/c/todo/today/downloads/platform-tools/adb.exe devices

# APK 설치
/c/todo/today/downloads/platform-tools/adb.exe install -r C:/todo/today/SensorMonitor/app/build/outputs/apk/debug/app-debug.apk

# 앱 실행
/c/todo/today/downloads/platform-tools/adb.exe shell am start -n com.example.sensormonitor/.MainActivity
```

---

## 기술 메모

### YUV420 Row Stride 문제
- Android CameraX는 디바이스마다 다른 버퍼 레이아웃 사용
- Row stride가 width보다 큰 경우 패딩 바이트가 있음
- Galaxy M53: row stride = width (패딩 없음)
- Galaxy A23: row stride > width (패딩 있음)
- 범용 코드는 row stride를 항상 고려해야 함

### Build 클래스로 디바이스 정보 얻기
```kotlin
Build.MANUFACTURER  // "samsung"
Build.MODEL         // "SM-A235N"
Build.BRAND         // "samsung"
Build.DEVICE        // "a23"
Build.PRODUCT       // "a23ksxx"
```

---

*작성일: 2026-01-13*
